const repetir = 'repiteme';

//Se le puede asignar antes de mostrarlo
const repeat = 'repiteme'.repeat(2.4);

//Cantidad de veces a repetir cierto texto
console.log(repetir.repeat(3));
console.log(repeat);

//Separar por el signo indicado
const hobbies = 'Leer, caminar, escuchar musica, escribir';
console.log(hobbies.split(','));